<h1>会員詳細ページ</h1>

<div>
  <%#= link_to "編集", edit_admin_user_path(@user.id) %>
  <h1>氏名：<%= "#{@users.last_name} #{@users.first_name}" %></h1>
  <h1>フリガナ：<%= "#{@users.last_name_kana} #{@users.first_name_kana}" %></h1>
  <h1>メールアドレス：<%= @users.email %></h1>
  会員ステータス：<% if @users.is_active ==true  %>
                  有効
                   <% else %>
              　　退会
                  <% end %>
  <% if admin_signed_in? %>
    <ul>
      <li>
        <%= link_to '強制退会', admin_user_path, method: :delete , data:{confirm: "本当に退会させますか"} %>    
      </li>
    </ul>
  <% end %>
</div>
